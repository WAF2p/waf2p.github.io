# Example Environment Configuration for WAF++ Website
# Copy this file to .env.local and customize if needed

# Path to local framework repository
# Default: ../framework (assumes framework is cloned as sibling directory)
# Uncomment and modify if you use a different directory structure:
# LOCAL_FRAMEWORK_PATH=/path/to/your/framework
